#include <stdio.h>

int main(){
	char e = 'excesso';
	float p, m;

	printf("Informe o peso dos peixes:  ");
	fflush(stdout);
	scanf("%f", &p);

	if(p > 50){
		m = (50 - p) * 4;
		printf("O seu peso ideal seria %.2f", peso_ideal);

	}else{
		if(tolower(sexo) == 'f'){
		peso_ideal = (62.1 * altura) - 44.7;
		printf("O seu peso ideal seria %.2f", peso_ideal);

	}else{
    	printf("Dados indisponíveis. Por favor, escolha sexo m ou f.");
    }




	}
}
